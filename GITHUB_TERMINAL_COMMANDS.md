# GitHub Terminal Commands

cd ~/Downloads/EmergencyWaitlistMatchingSystem_Local

git init
git branch -M main
git add -A
git commit -m "feat: add QueueMatch Local emergency waitlist matching system"
git remote add origin https://github.com/<username>/emergency-waitlist-matching-system.git
git push -u origin main
